@model IEnumerable<ONE_SMALL_STEP.Data.MessageType>

@{
    ViewBag.Title = "MessageType";
}

<h2>MessageType</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MessageTypeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MessageTypeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MessageTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MessageTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MessageTypeID })
        </td>
    </tr>
}

</table>
